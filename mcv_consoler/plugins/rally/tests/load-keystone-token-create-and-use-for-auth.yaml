---
  Authenticate.keystone:
    -
      runner:
        type: "rps"
        times: {{ compute }}
        rps: {{ compute }}
      sla:
        max_failure_percent: 0
      context:
        users:
          tenants: 2
          users_per_tenant: 3
      sla:
        failure_rate:
          max: 0
